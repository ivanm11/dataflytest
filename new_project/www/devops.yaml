---
project: 'newproject'
# fabric
db: 'newproject'
server: ['root@datafly.net']
remote_path: '/home/datafly/newproject/'
# production version
host: 'newproject.com'
path: '/home/datafly/newproject/production'
# staging version
host_staging: 'staging.newproject.com'
path_staging: '/home/datafly/newproject/staging'
# static assets - edit and restart development server after
# type:
#   result filename:
#     - files to compile, concat, minify; relative to /www directory
less:
  public:
    - less/layout
    - less/pages
js:
  public:
    - js/layout
    - js/pages    
    - datafly/widgets/ajax
    - datafly/users/login