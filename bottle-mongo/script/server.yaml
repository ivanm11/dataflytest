---

# install nginx & uwsgi
# please, edit hosts (add new project group from /etc/ansible/hosts)
# please, replace newproject with new project code
- hosts: newproject
  connection: fireball
  user: root
  gather_facts: false
  vars:
    root_dir: '..'
    project: 'newproject'
  tasks:
    - include: $root_dir/starter/ansible/utils.yaml
    - include: $root_dir/starter/ansible/mosh.yaml
    - include: $root_dir/starter/ansible/nginx.yaml
    - include: $root_dir/starter/ansible/uwsgi.yaml
    - include: $root_dir/starter/ansible/mongo.yaml
  handlers:
    - include: $root_dir/starter/ansible/handlers.yaml