# STATIC ASSETS
# TYPE = { result: [files] }
# files - list of files to compile, concat, minify (relative to /www directory)

CSS = {
    'public': [
        'datafly/admin/layout',    
        'less/docs'
    ]
}
JS = {
}