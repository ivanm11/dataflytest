
# I understand that long path to ansible is confusing here,
# but it's still better than including "script/datafly" twice in repo

---
ansible: '../../new_project/script/datafly/ansible'
project_root: '..'

# EDIT BELOW

project: 'doc'
hosts: ['root@datafly.net']

# trailing slash is important
remote_path: '/home/datafly/doc/'

# production host
host: 'doc.datafly.net'